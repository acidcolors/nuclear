// РЕЖИМ РАБОТЫ КОНТЕНТА
// true  — данные подтягиваются из Notion (динамический режим)
// false — данные берутся только из локального файла src/data/products.ts (статический режим)

export const CMS_CONFIG = {
    USE_NOTION: true
};
